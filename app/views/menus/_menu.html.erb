<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t('application.name'), root_path, :class => 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= t('help.help') %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to t('help.download_manual'), download_manual_help_path %></li>
          </ul>
        </li>
        <% if user_signed_in? %>
        <% if current_user.has_role?(:admin) %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= t('admin.administration') %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= render 'menus/admin' %></li>
          </ul>
        </li>
        <% end %>
        <li class="divider-vertical"></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= show_gravatar ? (image_tag current_user.gravatar_url(:size => 20)) : current_user.full_name_comma %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to t('general.my_account'), user_path %></li>
            <li class="divider"></li>
            <%= render 'devise/menu/login_items' %>
          </ul>
        </li>
        <% else %>
          <%= render 'devise/menu/login_items' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>